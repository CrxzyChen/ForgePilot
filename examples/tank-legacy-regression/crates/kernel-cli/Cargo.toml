[package]
name = "ai-game-kernel-cli"
description = "Machine-readable command line interface for the AI Game Kernel"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[[bin]]
name = "kernelctl"
path = "src/main.rs"

[dependencies]
ai-game-kernel-core = { path = "../kernel-core" }
serde = "1.0"
serde_json = "1.0"

[lints]
workspace = true
