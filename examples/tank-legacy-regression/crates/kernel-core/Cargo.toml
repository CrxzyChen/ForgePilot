[package]
name = "ai-game-kernel-core"
description = "Deterministic, headless-first execution core for AI-authored games"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
jsonschema = { version = "0.52.1", default-features = false }
regress = "0.11.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10.9"
typify = "0.7.0"

[lints]
workspace = true
