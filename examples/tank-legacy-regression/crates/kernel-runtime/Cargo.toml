[package]
name = "ai-game-kernel-runtime"
description = "Native interactive 2D runtime for the AI Game Kernel"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
ai-game-kernel-core = { path = "../kernel-core" }
ai-game-kernel-renderer = { path = "../kernel-renderer" }
pollster = "0.4.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
winit = "0.30.13"

[lints]
workspace = true
