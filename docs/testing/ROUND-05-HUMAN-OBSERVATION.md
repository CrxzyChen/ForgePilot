# Round 05 human observation board

- Product: AI Game Studio 0.4.0 Copilot Preview
- Protocol status: P29–P32 machine phases passed at their documented scope;
  independent Journeys B–D are not yet assigned
- Latest independent run: Not started
- Latest internal observation: 2026-09-06 machine gates confirm capability-only
  media, review/import, hostile transport-error secret redaction across Jobs,
  audit and Copilot-visible results, addressable runtime observation, durable
  Completion Run recovery, and clean-profile credential/network/crash/
  migration/performance/package gates. The original game now has nine reviewed
  images and ten owner-accepted or owner-delegated-reviewed audio masters imported through audited
  ChangeSets. Native candidate WAV/MP3 playback and music/shot/hit event wiring
  are verified at their documented scope. Navigation and wall-destruction
  wiring have now also passed isolated review and original applied-file/test
  audits. The wrong stop-event Bus found during
  restart is now fixed and verified in current-revision normal-entry and natural
  loss/mute/restart logs. Final audio, long routes, short UI flows and both game
  package machine checks now pass at their documented scope.
  No participant has yet established
  that these controls are understandable without coaching.
- Release decision: Product-owner accepted and R5 closed on 2026-09-06.
  Independent P33 acceptance is deferred, NOT_RUN, not passed. This is not an
  independent participant signature. Decision: `../rounds/ROUND-05-ACCEPTANCE.json`.
  Accepted repair: Studio `0520a5c0…`, Tank `27bc3a91…` / Release `ffffa361…`.
- Development source: `docs/rounds/ROUND-05-COPILOT-GAME-COMPLETION.md`
- Checklist source: `docs/rounds/ROUND-05-CHECKLIST.md`

This file is the human-readable observation source of truth for Round 05.
Automated tests, screenshots, model transcripts, generated resources, and
developer demonstrations are supporting evidence only. They cannot populate a
participant result or close an unassisted human gate.

Blank issue counts mean no qualifying observation has occurred. They never mean
zero defects.

## Current observation dashboard

The full isolated owner-source regression completed successfully, including
portable packaging and installed-style lifecycle. Its archive is `3b75b645…`.
Release source `f41d6c02d037e8b724c0d6463322c5c20551dd14` is frozen in
`artifacts/r5-release-source-GzKI1R/checkout`: 1,319 source files match the
original dirty working tree byte-for-byte. Dependency installation, a fresh
native build, full check and applicable R5 specialist gates passed. The original
branch/index and owner installation are untouched. Package eligibility now
requires the actual archive's source binding to match this clean checkout.
The candidate archive is `2ede62d7…` (289 files). The role-separated kit passes
with `finalCandidateEligible: true` and all real A–E results still `NOT_RUN`.
See `P33-CLEAN-CANDIDATE-EVIDENCE.md`. Nine historical PNG test inputs were
separately archived as `eefcb0a8…`; an initial missing-input failure is retained,
and the unchanged foundation test passes after those inputs are restored.

The completed project is independently archived as `0868333e…` (163 source
files), beside final Development `112c2b31…` and Release `95aac9da…`.
`r5-completed-project-export-nmj4eW` records exact hashes, isolated validation,
19 resource/provenance/license metadata records and credential-pattern scans.
Original source remains unchanged; commercial license clearance, physical
network isolation and independent human acceptance are not inferred.

Final-revision victory is verified: original Copilot's normal-menu-entry
1008-Tick recording ends with HP 2/5, score 600 and zero enemies. Both
1280×720 and 960×540 Studio/Player pairs match in state, frame bytes and all
106 audio events, with exactly one victory cue and no fallback or playback
failure (`P31-FINAL-PLAY-EVIDENCE.md`, `r5-final-checkpoint-audit-k2oX7c`).
Natural muted defeat (1408 Ticks) and restart (1418 Ticks) also pass both
resolutions with 189/194 matching audio events. Actual Development/Release
ZIPs pass independent extraction, 68/26 hashes, all 19 assets, clean-environment
native startup and all three long replays (`r5-recorded-package-review-JHDlG8`).
Release hash is `95aac9da…`. Menu/help/settings/pause/resume/return short-flow
evidence now passes all 12 pairs (`r5-final-checkpoint-audit-A6qL3A`). Final
Development `112c2b31…` and unchanged Release pass exact current ZIP/receipt
identity checks against the previously replayed package contents
(`r5-final-package-identity-7P3Z4Y`). Independent P33 acceptance is still not
started; these receipts do not establish independent play or network isolation.

One misleading approval wait was closed: old `d58c1003…` provenance was already
fully included by applied `2434a90d…`. Exact field/file checks preceded a
hash-bound superseded rejection; no game source changed. No linked proposal
remains awaiting approval (`r5-superseded-context-closure-e8fstR`).

Latest supervised checkpoint: original Copilot applied the final wall/win/loss
wiring at `d7ea102d…`. All 22 fresh original reports / 276 assertions match
the isolated deterministic audio/state review; three 600-Tick exactly-once
probes and rollback pass (`P31-FINAL-AUDIO-WIRING-EVIDENCE.md`). All 19 imported
assets (nine images, ten audio) retain valid source chains
(`r5-imported-provenance-audit-HqnLXy`). Per-sound audio approval is closed;
test-project choices remain delegated to the supervisor. Final recordings and
packages are complete; no new owner listening decision is required.
The hidden-modal repair passed full regression and was owner-installed as
`37fc0b2e…`, with 287 verified content hashes and a recoverable old installation;
installed UI and four runtime parity checks pass
(`r5-plan-layout-delivery-Etprqd`, `P32-AUTHORING-PUBLICATION-INCIDENT.md`).

Earlier supervised checkpoint: original Copilot completed UI back provenance
and event wiring at `2dd834ff…`. Exact rollback, 21 tests / 268 assertions,
24 deterministic probes and original report audit pass; see
`P31-UI-BACK-WIRING-EVIDENCE.md`.

Earlier import checkpoint: UI back `782d23e2…` is separately
owner-accepted, selected and imported by the original Copilot at `a73d171b…`
after exact rollback and 20 tests / 255 assertions. Original imported-file audit
passes (`r5-applied-import-audit-lklvDB`). The owner delegated subsequent test-project audio
review to the supervisor; stop repeating per-sound human audition requests.
This does not close independent Journeys A–E. Plan/turn status confusion and
narrow-window dock clipping are repaired and pass full qualification in
`d632ef94…`; see `P32-PLAN-PRESENTATION-EVIDENCE.md`.

Historical retry checkpoint: the accepted 100ms confirmation sound, provenance
and event wiring are applied by the original Copilot at `cdffc27d…`. The
isolated 20 tests / 255 assertions and 14 deterministic probes pass; the
actual post-apply audio-session report also exactly matches reviewed source,
state and audio (`r5-confirm-runtime-audit-9aWMDT`). UI back was subsequently
accepted, imported and wired; its earlier audition wait is closed.
The old Studio exited normally for an update. A real resource-panel regression
repeated the same audio preview five times; the hash cache and single-flight
polling repair passes WAV/MP3/image/P15 UI gates. Full `npm run check`, package
qualification and actual owner installation now pass. The current package is
`87cb8897…`, with 287 verified content hashes, installed 150% DPI UI checks,
nine single-load image previews and four exact old/new runtime replay cases.
Evidence: `r5-polling-delivery-grO5uf`; see `P31-ASSET-PANEL-REFRESH.md`.

Earlier metadata checkpoint: original Copilot repaired three early audio
Skill/Brief context links at `b6e76869…`. All 15 imported assets now pass the
read-only provenance audit (`r5-imported-provenance-audit-NBbTt9`), with exact
isolated apply/rollback and original applied-file verification. This changes
neither sound bytes nor gameplay. The annotation explicitly distinguishes
post-import context from provider inputs. UI back was selected later; see
`P31-IMPORTED-PROVENANCE-AUDIT.md`.

The **owner-closed Studio retry recovered the original Copilot**. Native
Continue resumed the same conversation: navigation import, contextual
provenance and reviewed event wiring are now applied. The last exact original
audit matches revision `77be28e9…` after wall repair; see `R5-COPILOT-NATIVE-HANDOFF.md` and
`P31-AUDIO-MASTER.md`. This supersedes the 12:28 native handoff failure.
The outer framework Goal is now **active**, as verified at
`2026-09-06T05:09:45Z`; earlier blocked checkpoints below are historical.
All 20 actual post-apply reports / 255 assertions at `77be28e9…` match reviewed
module/state/audio identities (`r5-hit-postapply-audit-7kaSNB`).
This is not R5 completion or independent human acceptance.
The earlier reviewer-feedback Studio package
`dbf2197c…` passed uninterrupted full regression and installed UI/recovery gates,
287 verified content hashes, provider diagnostic/length guards and explicit
custom AudioBus control APIs. Both `dbf2197c…` and previous `ae576cfe…` are
recoverably backed up; the current installed package is `87cb8897…`. The exact
native playback gate keeps external media blocked and does not select/import
test candidates or change original game files.

The **internal Tank Goal was resumed** through its native Continue control
after owner acceptance of the 680ms wall-destruction WAV `1ccd17c3…`. The
owning Studio persisted the selection and import `4e986416…`; its exact four
files passed isolated apply, 19 tests / 252 assertions and rollback before
approval. Evidence: `r5-audio-import-review-1Dq6sl/summary.json` and
`P31-AUDIO-MASTER.md`. Import/application and event wiring are distinct stages;
the approval is not itself proof of game integration. Copilot subsequently
applied the wall import and a separately reviewed provenance update. Its first
event-wiring proposal `4f6331a6…` was rejected: the new fixture fails at Tick 0
on a missing UI dependency. Structured feedback is now persisted for Copilot to
read. Copilot consumed the reason and authored replacement `f1a02d7e…`.
It passed 20 tests / 255 assertions, destruction/nonfatal/idle probes and exact
rollback; Copilot applied and tested it. Actual original revision and persisted
wall test match review (`P32-CHANGESET-REVIEW-FEEDBACK.md`).
There was one successful provider attempt for the new wall prompt; old failed
Jobs were not retried. The owner accepted the new 100ms UI-navigation WAV in
the specific asynchronous audition reply. Studio persisted decision `32f8a9b1…`
and proposed import `3a881629…`. Its exact isolated four-file transaction,
19 tests / 252 assertions and rollback pass (`r5-audio-import-review-Rn16Ye`),
and original Copilot has now applied it and contextual provenance `e7371f19…`.
Navigation wiring `33af40be…` separately passes exact transaction/rollback,
19 tests / 252 assertions and 11 repeated event probes. The original applied
file/revision matches that review (`r5-ui-nav-wiring-applied-audit-aURXr7`).
The same Job reached a candidate on attempt three. The single victory-stinger
Job exhausted its three attempts
with `fetch failed` and no candidate. No duplicate Job or extra paid retry was
created. Native handoff has recovered without sending a new composer message;
wall fixture repair and navigation wiring are now applied and verified.

The reviewer feedback follow-up now has a desktop reason dialog, legacy
feedback supplementation and live review-list updates. Real development
Electron checks pass cancellation/focus, actual IPC rejection, reload history,
concurrent-review draft recovery and 150% minimum-window geometry
(`p32-review-feedback-ui-9CKsKI/summary.json`). The frozen implementation passed
full `npm run check`, `check:p32` and isolated installed lifecycle in variant
`r5-review-feedback-ui-check`, archive `dbf2197c…`; all 287 content hashes match.
Full-run P15 evidence is `p32-review-feedback-ui-lXOc6G/summary.json`.
The owner installation has now been replaced after normal Studio shutdown;
all 287 files and the recoverable old installation were verified, and the new
kernel/host passes all 20 game tests / 255 assertions with exact prior-runtime
parity (`r5-review-feedback-owner-check-2gRhYr`). Actual installed P15/P32
quality, 150% geometry, rejection/history/conflict and crash recovery pass
(`r5-installed-review-ui-DI96Ov`). The original Tank project was reopened.
This closes reviewer-UI owner delivery, not independent P33 acceptance.

The owner separately accepted the 100ms UI-confirm WAV `797ecb2f…` / SHA
`c5dbf06a…`. Its source, derivation and measurements pass technical audit
`r5-ui-confirm-master-review-Fy76jm`; the historical creation Player and current
installed Player reproduce identical output bytes. Native Studio recorded
selection `43059050…` and import proposal `668078c8…`. Exact four-file isolated
import, 20 tests / 255 assertions and rollback pass (`r5-audio-import-review-6eoD3d`);
supervisory approval `r5-ui-confirm-import-approval-Sp2JVu` does not apply game
files. Original Copilot applied the import: all four after-hashes, WAV,
manifest and provenance match at `01e71668…`
(`r5-confirm-import-applied-audit-DbWsli`). Contextual provenance supplement
`144f6c48…` passes exact semantic-delta and transaction/rollback checks and is
approved (`r5-confirm-provenance-review-OzPAXS`); its application and confirm
event wiring remain to be verified. Skill/Brief references are context, not
proof of additional provider inputs.
The scoped grant `generation-grant:r5-owner-20260905` expired at
`2026-09-06T05:14:56Z`. Under the user's existing R5-only paid-work authority,
successor `generation-grant:r5-owner-20260906` now expires at
`2026-09-07T05:32:34.909Z`. Audit `r5-scoped-generation-renewal-QH2zOr` confirms
the old grant, global approval policy, other projects, Job store and original
revision are unchanged; inactive Goals remain excluded. No provider call was
made by renewal, no budget/attempt counter reset and no failed Job duplicated.

Original Copilot applied music/shot wiring `e14d528c…` and the explicitly
owner-accepted 180ms hit-master import `421f555b…`. Their precise reviews,
19 tests / 252 assertions, actual audio events and rollback evidence are in
`P31-AUDIO-MASTER.md`. The hit-event proposal `cd60a2db…` separately passed all
19 tests, five deterministic audio-event replays and exact transaction rollback.
Original Copilot applied it and all 19 post-apply reports / 252 assertions match
the reviewed module, state and audio identities at revision `2865692b…`. No new
provider call or supervisor game-source edit was needed for these steps.

Remaining: other required UI/match sounds, audio-complete current-revision
recordings, standalone packages, full-game listening/play and independent
Journeys A–E. Individual owner candidate listening is not an independent P33
journey or proof of final mixing/seamless music looping. The old pre-audio
packages below are historical evidence, not the finished R5 game.

New engine follow-ups:

- Real loss/mute/restart comparison `0e90e135…` reports an undeclared Bus for
  stop at Tick 2037. The explicit-Bus repair passes P24 fixtures, compiled SDK
  discovery, full packaging and owner installation. Original Copilot applied
  approved migration `5003f5bc…`; all 19 actual reports / 252 assertions at
  revision `20eff586…` pass the read-only identity audit. New logs `f4df3d42…`
  (84 Ticks) and `07b9b5c3…` (2233 Ticks) pass both resolutions: all eight actual
  Studio/Player observations have no missing/failed audio or diagnostics,
  correct buses and matching full audio arrays/frames. Evidence is
  `r5-runtime-audio-audit-UX50k7` with frozen source documents, PNGs and logs
  (`P24-AUDIO-CONTROL-BUS.md`).
- Safe provider diagnostics and the ElevenLabs 450-character preflight pass
  P29 plus full regression/isolated installation of candidate `7dd82226…` and
  are included in the owner-installed combined repair `ae576cfe…`.
  The failed wall/UI prompts have 474/506 characters; the discarded provider
  response does not establish the exact cause. Old failed Jobs remain untouched
  and were not retried (`P29-PROVIDER-ERROR-DIAGNOSTICS.md`).
- Original Copilot recorded a normal-entry victory at Tick 3843, enemies zero,
  score 0600 and HP 4/5, without fixture state injection. Log `16126d4d…` is
  bound to revision `2865692b…`; it is pre-Bus-fix evidence, not final acceptance.
- The replacement current-revision victory log `361ca259…` reaches Tick 6202,
  score 600, enemies zero and HP 2/5. Both-resolution actual Studio/Player
  comparisons pass with 540 identical audio events, correct buses and no
  missing/failed playback or diagnostics. Frozen audit `r5-runtime-audio-audit-TWzqqk`
  and native state audit `r5-victory-state-audit-W7SppW` bind the result to
  revision `20eff586…`; both victory frames were inspected. Remaining sound
  coverage and independent acceptance are unchanged.
- Full `npm run check` passes again for isolated candidate `4a670004…`,
  including clean install, upgrade and crash recovery. The owner installation
  remains `ae576cfe…`; no reinstall, original source edit or paid generation
  was performed for this follow-up.

### Earlier supervised checkpoints (superseded where noted)

After the 2026-09-06 UI and direction review, installed owner Studio was
`6c4d0ffc…`, verified against all 287
content hashes after full check, package/upgrade/recovery and installed Engine
MCP assertion probes. Prior installations are preserved as recoverable backups.
Original Tank Copilot applied the direction, containment and row-spacing
repairs; current game revision is `17ae462e…`, with 19 actual-project tests /
246 assertions passing, including 76 double-resolution containment assertions.
The supervisor did not author or apply the game changes.

Original turn `01a07296-6c8f-7391-a77d-145eec1e9d73` recorded normal-Menu-entry
loss (Tick 1312), victory (Tick 952, six enemies cleared, score 600, HP 2/5) and
muted restart (Tick 1320). All three pass the supervisor's read-only
Studio/Player two-resolution audit: matching state, drawables, audio-event
identity and actual PNG hashes; all six final-state frames were inspected.
Development ZIP `0f140cfc…` and Release ZIP `4ae391a8…` were independently
extracted and verified, including all three recorded routes, both resolutions,
startup and five native GPU frames. Evidence:
`artifacts/r5-recorded-package-review-or8dUZ/summary.json`. These remain pre-audio
packages, not complete-game releases. Formal SFX and independent acceptance
remain open. The desktop task summary's
`notLoaded`/`interrupted` is not the live state of this Studio-owned execution.
See `P31-SUPERVISED-TANK-PROGRESS.md` for exact logs and evidence. Older game
logs/packages below are historical, not current-revision acceptance.

The next engine increment repairs durable test-result feedback and adds shared
UI/MCP verification of an existing Windows ZIP. Source/MCP and real Electron
tests passed; final compact report UI, full isolated candidate regression and
owner installation are still in progress. The original internal Goal is
currently blocked, preserving revision `17ae462e…`; it must be resumed through
Studio after delivery, not by starting a competing executor. The proposed final
game documentation ChangeSet was rejected because applying it would invalidate
the just-recorded conservative project revision. Root evidence remains intact.

Latest audio correction: candidate `aa63a769…` passes full check and isolated
install/upgrade/recovery, P29 decoded metadata/import/agent tests and WAV/MP3
package checks. The prior Player could not decode the production route's MP3,
and metadata was request-derived; both are repaired. Fake/damaged audio is
rejected and a corrupt legacy job cannot crash history recovery. All 287 content
hashes are verified. At that checkpoint the owner installation was `aa63a769…`
(now superseded by `6c4d0ffc…`), and its installed MP3/WAV inspector and
invalid-media negatives pass; no supervisor game-file
edit or paid call occurred.
See `P29-AUDIO-CODEC-FOLLOWUP.md`. Formal SFX, game repairs and P33 remain open.

Historical native-window blocker, subsequently repaired in the installed build:
ordinary launch of the replay-verified Release
crashes in the real GPU pipeline, and the native key router omits P/H/O/M and
bracket keys. A further late-phase lifecycle queue loss hid the pause overlay.
Source repair passes 14 Player/7 host tests, ten GPU-pixel checks and complete
split/continuous Studio snapshots. A real diagnostic window now displays pause,
help and audio transitions, mute/unmute and 80→70 volume; maximize renders
without a GPU error. Audio instructions still crowd their decorative frame.
Full `npm run check` and P30 now pass. The 287-hash-verified Studio candidate
`81201a24…` was installed at that checkpoint and is now superseded by `aa63a769…`.
The handoff and four-way repair are now complete at the bounded scope above;
replacement game ZIPs remain open.
The 22:10 read-only original-project-copy probe confirms Up/Down are reversed
on screen and all four illustrated muzzle/projectile directions oppose actual
movement/velocity (dot product −1). Original authority is unchanged. Evidence:
`artifacts/r5-direction-review-xyr0lE/summary.json`. At that earlier checkpoint,
Studio capture failed twice with `foreground window did not report a process id`.
A fresh Computer Use session later restored access and the handoff in
`R5-COPILOT-NATIVE-HANDOFF.md` was delivered without private UI/IPC workarounds.
See `P30-NATIVE-PLAYER-WINDOW.md`. Earlier offscreen replay results remain valid
at their stated scope but are not proof of windowed play.

At 19:28 local, internal Copilot's post-layout regression passes all 14 actual
project tests / 96 executable assertions, with individually verified report
hashes. Pause/result title and button bevel-spacing repairs are applied;
the reviewed 960x540 pause frame has readable inset title placement. Actual
all-state after-frames, formal sound assets, Player parity and final independent
packages remain open. See `P31-SUPERVISED-TANK-PROGRESS.md` for the exact baseline.

The bounded-checkpoint Studio build passes the complete `npm run check` and
`check:p30`, including installation/upgrade/crash recovery and actual Electron
UI gates. ZIP SHA-256 is
`ce2b0287f667617b4359acca37a60a2cb3f763dab7e7c1a144ab663e19fc0ea7`;
the 100-run Tank example takes 39,120.86 ms. Its new `runtime.advance_ticks`
advances paused sessions by at most 200 Ticks, retains every snapshot and
rejects stale/duplicate requests without relaxing execution budgets. A diagnostic
copy of the actual game matches all 400 continuous snapshots and reaches a
real loss state at Tick 860 in bounded requests. This is engine evidence, not
the actual Copilot's final game acceptance. At 19:29 the old canonical Studio
was closed normally and recoverably backed up; all 287 candidate content hashes
match the installed copy. The supervised Copilot handoff is in progress.

That handoff subsequently succeeds: actual Copilot uses the paused batch API,
reaches real Arena loss at Tick 960 and catches a failed same-Scene restart.
The supervisor reproduces the defect: resumed live state overwrites the authored
Scene library, causing reload to retain the dead player world. The source fix
passes continuous/split-request regression, complete check, P30 and installed
recovery gates. Package `8798eef9…` is installed with 287 content hashes checked
and the prior tree backed up. Original Copilot then verifies actual loss→restart
at Tick 1616: HP 5/5, six enemies, score zero, muted=true and volume=80. The
supervisor checks actual state and frame. Normal-input victory at Tick 2340 is
also separately established.

Copilot's subsequent three-file HELP/result inset repair is applied. Four
affected tests / 44 assertions pass; the supervisor checks HELP and actual
loss after-frames. A later two-file HUD proposal moves bottom hints above the
decorative border, retains every assertion and adds one. Its four affected
reports pass, and supervised actual play/HELP/pause frames confirm the inset.
The later durable-input/guarded-Player repair passes a 551-Tick recorded combat
comparison and fixes omitted packaged Prefab definitions. Complete check,
P30 and post-checks pass. Package `19410ce9…` is now installed with all 287
content hashes independently checked and the previous tree recoverably backed
up. The same Copilot acknowledges the handoff, applies the HUD repair and
exports its actual ten-Tick play/HELP/pause log. Both viewport comparisons
match recorded/Studio/Player state and PNG hashes. Pre-upgrade sessions cannot be
retroactively given an input log. The actual 1408-Tick Menu→loss→R/restart log
subsequently matches both hosts at both viewport sizes; eight preserved
snapshots also match through lost→ready→playing. Victory attempts expose a
separate end-state input defect: fire after player destruction can be mistaken
for Menu input and restart Arena. Copilot adds a failing test, then applies
reviewed repair `8fa5cd5a…`: lost/won SPACE and explicit R/ESC all pass.
The supervisor counts 17 post-repair reports and 107 assertions, all passing.
The repaired revision's actual 977-Tick Menu/loss/terminal-fire/muted-restart
recording also matches both host states and PNGs at 960x540 and 1280x720.
Native intermediate states retain defeat after SPACE and restore a fresh match
with mute retained; master-bus events at mute/restart are independently checked.
The subsequent real 1175-Tick victory recording also matches both hosts at
both viewport sizes. The final 175 actual Studio/Player snapshots, including
playing→won, match independently; zero enemies, score 600 and HP 2 remain
unmodified gameplay outcomes. Copilot's subsequent Development and Release ZIPs
are independently extracted, manifest/hash checked and replayed against both
current recordings. Both native packages contain six Prefabs and nine images,
reach the recorded state hashes and produce the same eight two-resolution
frames. Development ZIP is `3b80cfed…`; Release is `13011e00…`. Runs exclude
Node/Codex/provider environment, but do not claim OS network isolation or
independent play. Formal audio and final audio-complete release remain open.
Machine success is not independent P33 success.

The owner's scoped paid-generation permission remains effective for R5,
including disclosed unknown prices. The existing ElevenLabs production SFX
adapter lacks a credential reference; this is not another approval request.
After three unchanged provider-health checks the internal Copilot Goal reports
blocked and preserves its completed work. A Bailian video-derived alternative
has only been researched, not implemented, charged or accepted; see
`P29-BAILIAN-SFX-FEASIBILITY.md`. No TTS imitation, test tone or invented cost
is accepted as formal SFX. Independent participant Journeys A–E remain unsigned.

### Earlier supervised checkpoints (historical evidence)

Latest supervised game checkpoint (2026-09-05): nine formal images are imported
with verified hashes and Skill/Brief provenance. All nine now have runtime
wiring, including the menu background and UI atlas. These are not visual passes.
The player/enemy/projectile/destructible-wall Prefabs and Scene ground are wired;
solid-wall wiring is applied and its four boundary colliders are unchanged.
The vertical wall crop is repaired and checked in an actual frame. VFX is wired,
with fixture-frame transparency checks; final arena presentation is still required.
The pre-UI-migration eleven-test baseline has 68 passing assertions: UI flow 13, UI safety five,
win/loss/friendly fire eight, command routing four, smoke eight and Collider
offsets eight, Scene audio-state transfer twelve, and VFX lifecycle/impact ten.
Actual master-bus events retain 0.7 volume/mute true on all six tested transitions.
The live split-request checkpoint matches the continuous state hash; formal
audible output remains pending. Deliberately wrong score and pause/UI expectations fail correctly;
temporary fixtures are moved into recoverable history, with failed reports retained.
The initial hidden-label failure, its targeted fix and passing UI-safety rerun
are documented in `P31-SUPERVISED-TANK-PROGRESS.md`. Ground coverage and initial
overlay occlusion are repaired; final HUD styling, remaining art, formal audio,
final regression and standalone delivery stay open. Menu and UI candidates have
been selected, imported and wired without repeat generation; actual 720p/540p
UI frames expose overlapping controls and unsupported separator glyphs. Eight
new UI assertions pass, but final whole-project and visual checks remain open.
A misleading UI-background judgment was corrected using PNG
alpha and Studio checkerboard evidence, not silently turned into a paid retry.

The earlier complete `npm run check` passes in the independent
`r5-scene-transfer-check` output, ZIP
`766d3897f55b98323f9f39ced3dc21477153991a3c6f87df60b6717dfff3da76`.
It includes deterministic Scene-state transfer and missing bitmap glyph repairs,
plus clean-package/installed lifecycle/UI checks. The supervisor closed the old
Studio normally, retained its complete package as a backup, verified all 286
manifest-listed hashes in the new canonical copy and started the new Studio.
Actual Copilot adopted the new API through SDK and game-logic ChangeSets; its
continuous and split-request checks pass at the documented supervised scope.
Engine checks alone do not mean formal game audio is complete. See
`P21-SCENE-TRANSFER-EVIDENCE.md` and `P31-SUPERVISED-TANK-PROGRESS.md`.
The previous `r5-prefab-check` full pass and its 57,668.11 ms/100-run result remain
historical evidence, not the new build's benchmark number.
The follow-up full command `r5-host-profile-check` failed P20's unchanged
60-second Tank batch gate. Its instrumented rerun measured 62,475.54 ms and also
failed. A content-addressed, per-runtime compiler cache and invalidation checks
now pass the targeted P20 rerun in 56,808.14 ms. The next full command passes the
runtime stages (56,054.96 ms for Tank) and builds ZIP `7da6c517…`, but fails its
last extraction step because C: Temp is full; it is not a full pass. The unchanged
installed check passes with D: TEMP/TMP, including upgrade and crash recovery.
Old inactive installation-test copies are recoverably archived on D:. A new full
command with the UI Skill regression now passes completely: 288 files, ZIP
`fd0a174a…`, including clean installed lifecycle and a 58,574.95 ms Tank batch.
It has not replaced the running canonical package. The separate P30
development-interpreter repair passes its complete targeted gate; see
`P30-DEBUG-HOST-PERFORMANCE.md`.
The subsequent native debug-history repair passes native tests, strict Clippy,
actual-project-copy parity and a complete P30 rerun. Under the original 64 MiB
project budget, 260 continuous Ticks now succeed with all snapshots retained;
their contents match the old host exactly. Full delivery regression now passes
for ZIP `2d68aba9…`: Tank's 100-run batch takes 29,026.51 ms, all 287 listed
content hashes match, and installed upgrade/crash recovery/uninstall gates pass.
At 19:00 local this new package replaced the canonical Studio after normal
shutdown and a recoverable old-package backup. All 287 copied content hashes
match; the actual project, conversation and Goal reopened. The input-protection
interruption is resolved, and the new-host diagnosis was sent through Copilot.
This is supervised delivery, not independent participant evidence.
Copilot's reviewed UI corrections expose muted state,
separate HUD badges and fit button labels; the latest actual play frame is
reviewed, but all-state visual acceptance and final project regression remain
open. The muted/Help, UI-safety and UI-flow post-apply tests pass 10/7/13 assertions.
The actual loss-screen title/button repair and missing-fixture-object follow-up
are applied. Its eight-assertion loss fixture passes; actual win/loss frames
and final regression remain required. A read-only credential-summary check finds only OpenAI
and Bailian; no formal SFX credential has yet been supplied. No secret was read.
The earlier output-directory collision and serial recovery remain documented in
`P17-PREFAB-IDENTITY-EVIDENCE.md`, not silently rewritten as an earlier success.

Earlier checkpoint: the long-history and import-relink repairs pass their targeted tests.
Full regression reached P20 but failed its unchanged 60-second Tank batch
threshold. Follow-up packaging and installed lifecycle/UI verification pass
for ZIP `7234d1d37e4638c4318b692d5438e1ebba5c292ee0168707e79158b9ccc0a46f`;
the separate P20 rerun passes with 100 Tank runs in 54,638.28 ms. This does not
rewrite the failed full-command outcome. The existing Goal resumed automatically;
the installed panel shows four imported Jobs and its older-history button works.
The dashboard's engine/output watch-scope issue
was also repaired; local HTTP is healthy. Hosted lookup still returns
`Sites project not found`, so no new hosted publication is claimed.

Previous verified engineering checkpoint (2026-09-05 07:25 UTC): Studio package
`90d3e5da…` passes full `npm run check`, the rebuilt P32 MCP gate, and clean
installed lifecycle/UI verification. It includes Windows Git-watch feedback
filtering, atomic ChangeSet record replacement, usable long-transcript Goal
layout, compact MCP results with durable test reports, and correct failed-test
classification. The same installed Tank project and conversation are restored;
long-session responsiveness remains under observation, not independently signed.

At that earlier checkpoint, the user's project had four real generation jobs (player, enemy, projectile,
destructible wall), each with one attempt and two PNG candidates. Player and
enemy imports are applied; projectile candidate 2 is selected, not yet imported;
wall review is pending. Actual charges remain unknown. Copilot applied collision
batch `bddb8457…` and reported X/Y offset checks; UI batch `712b34ea…` is applied,
but its rerun found a help-text tick mismatch. The follow-up `409c8458…` is
reviewed and approved, not yet accepted as a passed game feature. Nine stale
proposals were rejected without deleting their contents or candidate choices.
Formal sound-effect routing, Sprite/Prefab wiring, remaining gameplay tests,
standalone game delivery and independent human acceptance remain open.

Internal dogfood update (2026-09-05): the installed Studio Copilot has now
started the user's existing Tank project, audited it through Engine MCP, and
submitted planning ChangeSet `changeset:72c688ce-f4eb-43bc-8640-341bea7fb07a`.
Its four new files cover art/audio direction, placeholder inventory, asset
briefs, and completion coverage. Following the owner's explicit delegation,
the supervisor approved and applied this proposal through Studio; Copilot
confirmed project validation and missing-resource diagnostics were clear.
It executed eight discovered tests and a
140-Tick gameplay replay; missing prerecorded per-Tick hashes remain an
explicit evidence gap. Copilot created image job
`asset-job:2e3558e7-73be-4e5d-ae61-4ba70bf24375` with two OpenAI player-tank
style candidates requested; the job was waiting for policy authorization,
not yet generated, at this checkpoint. Unknown price is not a zero-cost claim.

Copilot also proposed UI/controls ChangeSet
`changeset:154a9716-30e2-4d83-bd28-c047fa508729` and diagnostic ChangeSet
`changeset:c1d761b6-d712-4c31-94d9-1f7fc1b66e77`. The UI proposal has been
reviewed and approved by the delegated supervisor. Installed Copilot then
applied it through `change.apply` after the transport repair; it is not yet
accepted as a finished feature. The owner delegated R5 review and necessary
spending on 2026-09-05; a time-limited, exact-project/thread/Completion-Run
generation grant now preserves the unchanged global approval policy.

Internal audit finding: the installed test loader ignored natural-language
`assertions`. Source now implements real state comparisons and replay-scene
selection; the red/green regression passes (`P31-RUNTIME-ASSERTIONS.md`).
Executable pause, volume, UI and win/loss assertions in the actual game remain
required before P31 completion.

The next supervised turn was restored in Goal mode but stopped at an MCP
elicitation. Studio dropped its message and used a legacy approval response.
No unknown request was approved. The turn was interrupted while protocol,
message and visible approval-space handling were repaired and revalidated.
Installed continuation now confirms `change.apply` returns `applied` without
a second context-free transport question. The original image job entered
`running` at 2026-09-05 05:59:37 UTC with one attempt and the exact delegated
authorization ID. Both real PNG candidates completed. The delegated supervisor
reviewed the images, transparent corners and hashes, selected candidate
`candidate:d98fbab2-279e-4658-9c4f-4cabd4d74d03`, and approved import ChangeSet
`changeset:3270141a-0553-41e6-a6cf-870052c82b80`. Installed Copilot applied it;
the job became `imported` at 06:07:59 UTC. The imported SHA-256 is
`0475f10ec66895eb5189f15a605a1e0ffd1f449cebc09818f59f530121494b09`.
Copilot subsequently applied reviewed provenance supplement
`changeset:43c95fa9-34e2-45e6-8e24-b1a394fbaac8`, binding the actual
art-direction Skill and asset-brief hashes. Runtime use and gameplay-scale
visual acceptance remain pending; import alone is not finished-game evidence.

Enemy job `asset-job:35052cbd-8f3a-4fc6-853b-532d109d7309` also generated two
real PNG candidates in one attempt. The supervisor selected the second,
`candidate:12393222-487a-491e-9f24-d4aa2825c84a`, after visual and transparent
corner checks. Its hash is
`e7af7deaa8096f65963e5bd009e55dc4d57549de066733054781f3d0ad1042cb`;
replacement import `changeset:67530b32-804a-41c2-96f4-41e95e0d1fd2` is approved,
with application still pending at this checkpoint. Both jobs have
unknown actual cost, not a claim of free generation.

These selections were performed by the owner's delegated supervisor. The
current UI records its default `human` source for selection; that field must
not be interpreted as independent participant evidence. No P33 human result
has been supplied or signed. The supervised continuation also rejected the
old pause/collision proposal as incomplete: it retained prose assertions and
needed nested-page return/own-collider-offset fixes. Installed `script.api`
now exposes the canonical SDK, so obsolete project declarations no longer
justify claims that AudioBus, playback or Prefab APIs are missing.

The later core proposal `changeset:2b8a006a-4084-46ff-b0ac-ec1179aefe46`
was rejected during delegated review: its Y-axis movement still passed only
Collider size, and the project had no declared master audio bus. New-file
creation also exposed raw ENOENT from an incorrect `project.file.write`.
The source diagnostic and long-session streaming/import-state churn repairs
passed P29/P32 regressions, full `npm run check`, and the installed lifecycle
gate. A newly packaged Studio is restoring the same project and conversation;
restoration alone is not proof of completed gameplay or independent recovery.

This user project is separate from the repository's Neon Bastion fixture and
its nine candidate previews. Those previews must not be counted as generated
or selected assets in the user's Tank project. The dogfood run also exposed
Plan/message/automatic-turn and checkpoint-churn defects; the new
`check:p32:progress` regression gate covers their repairs. This is a supervised
developer run, not an independent participant result.

| Journey                                   | Candidate            | Participant  | Status                                                                        | Blockers | Major | Minor | Evidence                                                                                                           |
| ----------------------------------------- | -------------------- | ------------ | ----------------------------------------------------------------------------- | -------: | ----: | ----: | ------------------------------------------------------------------------------------------------------------------ |
| P28 contract and mental model             | development checkout | not assigned | authority/schema contract passed; comprehension unobserved                    |        — |     — |     — | `P28-ROUND05-CONTRACT-EVIDENCE.md`                                                                                 |
| P29 media generation and reviewed import  | development checkout | not assigned | machine and secret/package boundaries passed; Journey B unobserved            |        — |     — |     — | `P29-MEDIA-JOB-EVIDENCE.md`, `P29-TRANSACTIONAL-IMPORT-EVIDENCE.md`                                                |
| P30 visual/runtime diagnosis and repair   | development checkout | not assigned | machine passed; Journey C comprehension unobserved                            |        — |     — |     — | `P30-RUNTIME-OBSERVATION-EVIDENCE.md`, `P30-DIAGNOSTIC-REPAIR-EVIDENCE.md`, `P30-STUDIO-PLAYER-PARITY-EVIDENCE.md` |
| P31 complete Tank transformation          | development checkout | not assigned | foundation, 9/9 previews and 9/13 review filter passed; human completion open |        — |     — |     — | `P31-TANK-FOUNDATION-EVIDENCE.md`                                                                                  |
| P32 interruption and budget recovery      | development checkout | not assigned | machine phase passed; Journey D unobserved                                    |        — |     — |     — | `P32-COMPLETION-RUN-EVIDENCE.md`                                                                                   |
| P33 clean-profile provider-to-package run | development kit      | not assigned | role-separated kit passed; final candidate/run not started                    |        — |     — |     — | `P33-ACCEPTANCE-KIT-EVIDENCE.md`                                                                                   |

## Candidate under observation

- Studio package: development candidate
  `AI-Game-Studio-0.3.0-preview.1-win-x64.zip`; not final 0.4.0
- Studio SHA-256:
  `90d3e5da55367a64cddd266128786acdde65de245e0448f6abf909448a5d37af`
- Source commit: `36427446f7a737f517ee94343c5bbc423fbce5b1`, dirty;
  `finalCandidateEligible: false`
- Placeholder Tank input-project hash:
  `58063022632d46d5632c2533385e91e1b5ef97237d2945d9104e1954d6c6add0`
  (source tree
  `f9bd0e24b8598c9f72d9e552dcf5a776993b4eb12b065a9809c239ae88ac8e15`,
  55 files)
- The existing independent acceptance kit pins the earlier `1dc209e15d44…`
  development candidate. It has not been rebuilt or run against this new
  supervised candidate; retain that distinction when validating its records.
- Completed Tank project hash: pending
- Development package hash: pending
- Release package hash: pending
- Provider test budget and account: pending
- Machine evidence index: P28–P30 and P32 machine evidence published, P31
  foundation evidence published; P31 completion and P33 independent evidence
  remain pending
- Participant task sheet: `ROUND-05-PARTICIPANT-TASKS.md`
- Observer run record: `ROUND-05-OBSERVER-RUN-RECORD.md`
- Structured result: `ROUND-05-OBSERVATION-RESULT.template.json`
- P29 result template: `docs/testing/P29-HUMAN-RESULT.template.json`
- P29 result validator: `npm run check:p29:human`
- Offline evidence validator: `scripts/validate-round05-human-evidence.ps1`;
  incomplete template rejection passed
- Human result: not started; `R5-OBS-001` remains open

## Observation question

Round 05 tests one product claim:

> Can a developer state a game-level outcome in Studio Copilot and remain in a
> clear supervisory role while project Codex uses structured engine tools to
> turn a playable placeholder game into a reviewed, tested, standalone game?

The observation must distinguish:

- **direction** — the human changes product intent or scope;
- **required gate** — the human approves cost, chooses a subjective candidate,
  or applies a ChangeSet;
- **clarification** — Codex asks for information genuinely absent from project
  authority;
- **coaching** — the observer tells the participant where to click, what hidden
  command to use, how to repair the engine, or how to bypass a failed step;
- **manual workaround** — the participant uses terminal, another IDE, direct
  JSON/file editing, private API calls, DOM tools, or screen automation to do
  work assigned to Copilot.

Direction and required gates are expected. Avoidable clarification is a
usability finding. Coaching and manual workarounds fail the affected step.

## Required participant and environment

Use at least one participant who did not implement Round 05 and has not seen
the current completion workflow. The final P33 participant must not have used
the tested Studio build or completed Tank project.

Provide only:

- a clean Windows profile or clean test machine;
- the hash-pinned installed Studio candidate;
- the hash-pinned shape-based Tank input project;
- the canonical written goal below;
- named provider credentials entered by the participant through Studio;
- a provider account with a disclosed, bounded test budget;
- written notice that provider calls may be paid and screen/voice may be
  recorded with consent.

Do not provide:

- the engine repository or development checkout;
- terminal or command-line instructions;
- another editor or IDE;
- private MCP/HTTP/IPC calls;
- direct project-file or job-store editing;
- DevTools, DOM selectors, coordinates, or screen automation;
- a preselected model/candidate unless the journey explicitly tests that
  product decision;
- verbal or chat coaching from the implementation team.

Record Studio version, package and project hashes, Windows version, display
scale, locale, provider/model/cost policy, start/end time, screen and voice,
Copilot transcript, tool calls, Goal/Plan state, jobs, approvals, candidates,
ChangeSets, tests, Replay, build reports, output packages, and issue notes.

## Canonical task shown to the participant

> Open the supplied Tank project. In Studio Copilot, ask Codex to upgrade the
> playable placeholder game into a coherent, polished, standalone 2D Tank game.
> It should establish an art direction, determine which visual and audio assets
> are required, use the configured generation tools, present paid calls and
> candidates for your review, import approved resources, replace placeholder
> presentation, complete the menu/HUD/help/pause/win/lose/restart experience,
> run and inspect the game, fix failures, run tests and Replay, and build the
> Windows Development and Release packages. Do not use a terminal, another IDE,
> direct file editing, DevTools, or screen automation. Make product, cost,
> candidate, and ChangeSet decisions when Studio asks you.

The participant may phrase this naturally. The observer must preserve the
intent and may not add implementation hints.

## Journey A — Goal, Plan, and tool-state comprehension

Starting state: clean Studio profile, Tank project unopened, credentials not yet
entered.

Tasks:

1. Open the project and identify its current playable and placeholder state.
2. Open Copilot, sign in if required, and submit the canonical goal.
3. Determine whether Engine MCP and required project Skills are connected.
4. Read the generated Goal and Plan and explain the current step.
5. Identify a missing credential/route or other seeded readiness failure from
   the visible diagnostic and repair it through normal Studio settings.
6. Stop before the first paid request, close Studio, reopen, and continue the
   same Goal.

Pass conditions:

- the first click and project/Codex connection have continuous visible feedback;
- the Goal describes the game outcome rather than a list of vendor API calls;
- the Plan is ordered, current, and distinguishes running, waiting, failed, and
  completed steps;
- Studio identifies the exact missing provider/capability/credential/model state
  without exposing a secret;
- restart restores the Goal, Plan, transcript, and next safe action;
- no work requires knowledge of MCP tool names, internal files, or engine code.

## Journey B — Media generation, approval, review, and import

Tasks:

1. Let Codex create the art-direction Skill, asset inventory, briefs, and
   generation plan.
2. Review one image estimate with per-call approval enabled; approve it.
3. Observe progress, inspect at least two image candidates, reject one, and
   select one.
4. Review the candidate-import ChangeSet, apply it, inspect its project
   provenance, undo it, and reapply or regenerate safely.
5. Generate and review one required sound effect through its capability route.
6. Cancel one queued/running job and retry one deliberately transient failure.
7. Change to a bounded budget policy through Settings and verify that a known,
   in-budget request proceeds while an unknown/out-of-budget request waits.

Pass conditions:

- Codex requests capabilities without requiring the participant to copy model
  IDs or provider endpoints into the prompt;
- every paid action shows provider/model, parameters, estimate or explicit
  unknown-price state, and the approval effect;
- candidates are visible/playable and remain distinguishable by stable identity;
- the human can compare, reject, select, cancel, and retry without leaving
  Studio;
- selected files, manifest metadata, references, and provenance enter through
  one reviewable ChangeSet and undo cleanly;
- restart/retry does not repeat a billed request;
- no credential appears in Copilot, project, Diff, audit, logs, or imported
  provenance.

## Journey C — Complete Tank transformation and repair

Tasks:

1. Allow the same Goal to generate/import the remaining approved asset set.
2. Let Codex update Prefabs, scenes, UI, scripts, and audio routing through
   ChangeSets.
3. Apply changes in understandable batches and inspect the running result at
   menu, active play, pause, win, and lose checkpoints.
4. Introduce the seeded broken-texture, incorrect-layer/pivot, off-screen-UI,
   missing-audio, and unreachable-package-resource cases one at a time using
   the provided normal Studio test control or fixture.
5. Ask Codex to diagnose and repair each problem.
6. Play movement, firing, collision, damage, destruction, score, pause, win,
   lose, and restart.
7. Review final resource coverage and verify no required gameplay object remains
   a debug rectangle or fallback.

Pass conditions:

- Codex can inspect actual frame/audio/runtime evidence rather than claiming
  success from file existence;
- diagnostics link to stable project resources and the correct repair location;
- presentation changes preserve collider and gameplay semantics;
- repairs are previewable, testable, auditable, and reversible;
- the complete game is understandable, readable, visibly coherent, and
  audibly responsive at all required checkpoints;
- no terminal, external editor, direct JSON repair, private renderer state,
  DOM/DevTools, or coordinate automation is used.

## Journey D — Interruption, resume, cancellation, and budget integrity

The observer schedules one interruption at each state over separate runs:

- provider request is in progress;
- paid call is waiting for approval;
- candidates are waiting for review;
- ChangeSet is waiting for approval;
- runtime/test is in progress;
- build is in progress.

Tasks:

1. Close or crash the tested process at the assigned state using the documented
   observation control.
2. Reopen Studio and the project.
3. Explain the restored Goal/Plan/job/ChangeSet state.
4. Resume, retry, cancel, or stop as appropriate.
5. Inspect provider job identity, cost accounting, project Diff, audit, and
   output artifacts.
6. Stop one active Goal and later remove its completed/cancelled presentation.

Pass conditions:

- every wait and interruption returns to one honest, actionable state;
- no paid provider idempotency key, import, ChangeSet, test, build, or package
  side effect executes twice;
- stop prevents future work but retains project changes, costs, and audit;
- remove affects only eligible Goal presentation and never deletes project or
  audit authority;
- unrelated participant edits survive resume, repair, and rollback;
- the participant never has to delete a lock file, edit a job store, or reset
  internal state manually.

## Journey E — Test, package, clean-machine play, and continuation

Tasks:

1. Ask Copilot to run project validation, resource/dependency diagnostics, all
   tests, deterministic Replay, and required visual/audio checkpoints.
2. Let Codex repair one seeded failing test and rerun the affected gates.
3. Build Windows Development and Release packages.
4. Copy both packages to a clean machine without Studio, Node.js, Codex,
   credentials, or network access.
5. Play the required menu-to-win and menu-to-lose loops in each package.
6. Inspect the machine-readable build report and package-content/security
   result in Studio.
7. Close and reopen the finished source project and make one small Copilot
   presentation change through the same review/test/build flow.

Pass conditions:

- validation, tests, Replay, visual/audio checkpoints, and package inspection
  report real per-item results and source locations;
- both packages start offline and match required Studio behavior;
- packages contain every reachable selected resource and no temporary
  candidate, credential, private provider response, development-only tool, or
  unreachable resource;
- the build report preserves engine/project versions, project/resource hashes,
  selected provenance, licenses/restrictions, and test evidence;
- close/reopen continuation uses the same project authority and does not depend
  on old chat context.

## Completion outcome rubric

The observer records each required outcome separately:

| Outcome                              | Result  | Evidence |
| ------------------------------------ | ------- | -------- |
| Existing gameplay preserved          | pending | pending  |
| Coherent reviewed visual assets      | pending | pending  |
| Required sound and audio controls    | pending | pending  |
| Menu/help/HUD/pause/win/lose/restart | pending | pending  |
| Copilot visual/runtime diagnosis     | pending | pending  |
| Deterministic tests and Replay       | pending | pending  |
| Studio/Player required parity        | pending | pending  |
| Offline Development package          | pending | pending  |
| Offline Release package              | pending | pending  |
| Credential and package security      | pending | pending  |
| Restart/idempotency/budget integrity | pending | pending  |
| No forbidden workaround or coaching  | pending | pending  |

`PASS` requires every row to pass. A visually attractive screenshot cannot
compensate for failed gameplay, security, recovery, or standalone execution.

## AI participation record

Record counts and identities rather than impressions:

| Field                                                  | Value |
| ------------------------------------------------------ | ----- |
| Goal ID and initial prompt hash                        |       |
| Plan version / completed steps                         |       |
| Participant direction messages                         |       |
| Codex clarification questions                          |       |
| Avoidable clarification questions                      |       |
| Engine MCP tool calls / failures / retries             |       |
| Provider jobs / capabilities / models                  |       |
| Estimated / actual / unknown cost                      |       |
| Required provider approvals                            |       |
| Candidate review decisions                             |       |
| ChangeSets proposed / rejected / applied / rolled back |       |
| Runtime sessions / captured checkpoints                |       |
| Tests / Replay / repairs                               |       |
| Builds / packages / hashes                             |       |
| Studio/Codex restarts                                  |       |
| Duplicate side effects                                 |       |
| Observer coaching events                               |       |
| Forbidden manual workaround events                     |       |

No target count is treated as universally optimal. The record exists to locate
friction and distinguish product direction from avoidable handholding.

## Observation rubric

Record each event with timestamp, journey/step, visible Studio state,
participant expectation, actual result, recovery, direction/gate/clarification/
coaching classification, severity, owner phase, and evidence link.

Severity is outcome-based:

- **blocker** — cannot safely complete without coaching, a forbidden workaround,
  secret exposure, duplicate billing, data loss, false success, or an unavailable
  required capability;
- **major** — completes only after repeated unexplained failure, misleading
  state, lost Goal/job/ChangeSet context, or a non-obvious recovery path;
- **minor** — completes independently but produces measurable hesitation,
  avoidable rework, inconsistent wording, weak comparison, or visual friction.

Any observer coaching fails the affected step. Visible contextual help,
diagnostics, project docs, and Skills shipped with Studio do not count as
coaching.

## Per-run record

| Field                                       | Value |
| ------------------------------------------- | ----- |
| Observation ID / journey version            |       |
| Participant / experience / consent          |       |
| Studio version / commit / package hash      |       |
| Windows / profile / display / locale        |       |
| Input and output project hashes             |       |
| Provider / named credential labels / models |       |
| Approval mode / test budget                 |       |
| Start / end / active / waiting time         |       |
| Completed steps / total steps               |       |
| Direction / gate / clarification events     |       |
| Questions / hesitations / wrong turns       |       |
| Contextual help used                        |       |
| Coaching / forbidden workaround             |       |
| Goal / Plan / tool / job / ChangeSet IDs    |       |
| Runtime / test / Replay / build IDs         |       |
| Estimated and actual cost                   |       |
| Duplicate-side-effect result                |       |
| Credential/package security result          |       |
| Studio/Player parity result                 |       |
| Development / Release package hashes        |       |
| Blocker / major / minor IDs                 |       |
| Final pass / fail                           |       |
| Observer and participant sign-off           |       |

## Gap ledger

Add one immutable row per finding. A fix updates status and evidence; it does not
delete or rewrite the original observation.

| ID         | Journey / step   | Observation                                      | Severity | Owner | Status | Fix evidence | Clean-profile retest |
| ---------- | ---------------- | ------------------------------------------------ | -------- | ----- | ------ | ------------ | -------------------- |
| R5-OBS-001 | Not yet observed | Independent Round 05 observation has not started | blocker  | P33   | open   | pending      | pending              |

## Release sign-off

Round 05 human acceptance passes only when:

- Journeys A–E have qualifying recordings and durable artifacts from the
  hash-pinned candidate;
- the canonical task is completed without coaching or forbidden workaround;
- required human gates are understandable and no unauthorized call or write
  occurs;
- the finished Tank project and both standalone packages satisfy every
  completion outcome row;
- every interruption case preserves identity, audit, budget, and exactly-once
  side effects;
- no credential or private provider response enters project or package
  authority;
- there are no open blockers and every fixed blocker passes a new clean-profile
  retest;
- the observer and participant sign the final result and all evidence hashes
  pass the offline validator.
